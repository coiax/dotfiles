" Python settings
setlocal tabstop=4 "4 space tab indent for python
setlocal shiftwidth=4 "Use < and > in Visual mode to [un]indent regions
setlocal smarttab
setlocal expandtab "use spaces instead of <TAB> character
setlocal softtabstop=4 "treats 4 spaces as one <TAB> when backspacing
setlocal autoindent "does what it says on the tin
setlocal complete+=k~/.vim/syntax/python.vim isk+=.,(
